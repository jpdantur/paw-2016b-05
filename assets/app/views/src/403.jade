extends layouts/layout

block content
	+navbar(true)
	div.container
		div.col-sm-12.text-center
			h1 403 - #{messageSource.getMessage('403.headline', [], locale)}
			a.btn.btn-primary.btn-lg(href="#{context.url('/')}")
				i.fa.fa-home
				span &nbsp;#{messageSource.getMessage('403.home', [], locale)}
	